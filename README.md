# Звіт task 2 cryptography
### З яких файлів складається:
***[strumok_tables.py](strumok_tables.py)***<br>
***[strumok.py](strumok.py)*** - реалізація шифрів «Струмок-256» та
«Струмок-512»<br>
***[generate_relations.py](generate_relations.py)*** - генеруємо в потрібному форматі взаємозв'язки для Autoguess<br>
***[relations.txt](relations.txt)*** - файл з взаємозв'язками <br>
***[output](output)*** - те що повернув Autoguess<br>
***[recover_state.py](recover_state.py)*** - відтворення точного значення guessed variables й завдяки ним атака
***

## Теоретичнi вiдомостi щодо атаки часткового вгадування (Анна Ахламова)
Ця атака використовується, коли пряме розв'язання системи рівнянь, що описує шифр, є занадто складним (має занадто високу обчислювальну складність).

Основна ідея:
Замість того, щоб намагатися знайти всі невідомі змінні внутрішнього стану одночасно, криптоаналітик або зловмисник:

* Вгадує (фіксує) значення невеликої підмножини змінних внутрішнього стану.

* Визначає решту змінних, підставляючи вгадані значення в систему рівнянь шифру.

 * Перевіряє несуперечливість отриманого стану за допомогою відомих бітів гами.

## Цiкавi приклади використання саме такого виду атак на iншi шифри в наукових статтях та на практицi (Анна Ахламова)

## Атака на Bivium

Концепція:Bivium описується системою поліномів над K = GF(2). Оскільки повний стан Trivium (288 біт) вимагає вгадування понад 80 змінних (що перевищує складність brute-force ключа), аналіз фокусується на Bivium.

Механіка вгадування:
* Стан: 177 внутрішніх змінних
* Дані: 180–190 бітів гами (keystream) достатньо для формування повної системи рівнянь.
* Оптимізація: Метод Гаусса (Gaussian elimination) виділяє 22 головні (pivot) змінні та 36 вільних (free).
* Складність: Вгадавши 38 змінних (36 вільних + 2 додаткові), решта 61 визначається автоматично. Це знижує складність до $2^{38}$, що значно швидше за перебір ключа ($2^{80}$).

Результати:Найвищу ефективність показав алгоритм slimgb (Gröbner basis) — ~130 мс на ітерацію. SAT-солвери у цьому конкретному випадку працювали повільніше.

## Атака на KeeLoq
Концепція:
KeeLoq (стандарт для автосигналізацій) використовує 64-бітний ключ та 32-бітний блок. Атака поєднує вгадування частини ключа з алгебраїчним відновленням решти стану.

Ключові етапи:

* Вгадування: Достатньо зафіксувати лише 16 біт ключа.

* Алгебраїчний крок: Решта стану відновлюється через систему рівнянь (кубічні рівняння FSM та лінійні LFSR).

* Оптимізація (Fixed points): Пошук пар "відкритий текст/шифротекст", де стан змінюється мінімально, що різко спрощує систему поліномів.

Порівняння інструментів:
На відміну від Bivium, для KeeLoq SAT-солвери (MiniSat, CryptoMiniSat) виявилися вдвічі швидшими за базиси Грьобнера:
* SAT-solvers: 12–30 мс на крок.
* Gröbner basis: 27–180 мс на крок.

Підсумок: Повна атака (з урахуванням часу на пошук фіксованих точок) триває кілька годин.

## Атака часткового вгадування на A5/1 (GSM)
Концепція:Атака типу guess-and-determine на шифр A5/1 базується на вгадуванні стану найменшого регістра R_1 (19 біт) та подальшому обчисленні станів R_2 (22 біти) і R_3 (23 біти) через відому гаму (keystream).

Механіка атаки:
* Фіксація R_1: Перебираються всі $2^{19}$ варіантів значень першого регістра.
* Фільтрація через гаму: Оскільки $KS[i] = R_1[18] \oplus R_2[21] \oplus R_3[22]$, знання R_1[18] та KS[i] одразу відсіює 50% хибних комбінацій для R_2 та R_3.
* Поступове відновлення: Через правила нерегулярного тактування та наступні біти гами визначаються критичні біти (clocking bits $R_2[10], R_3[10]$ тощо).
* Відсікання (Pruning): Кандидати на внутрішній стан, що суперечать потоку гами, видаляються.

Чому це "часткове вгадування":Замість повного перебору 64-бітного стану, ми вгадуємо лише 19 біт, а решту 45 біт обчислюємо як залежні змінні через систему рівнянь шифру.

Часова складність: $2^{48.5}$ операцій (у $2^{15.5}$ разів швидше за brute-force)
***

## Приклади використання власної реалiзацiї, тестовi вектори та результати тестування. (Нікіта Безверхий)

**Підготовка до реалізації:** Спочатку я адаптував файл з таблицями під python, оскільки у мене не було досвіду з C і ми домовилися весь проект писати на python.

**Логіка шифру** закладена в клас `Strumok`. При програмуванні цього класу було важливо зробити його максимально гнучким, щоб можна було реалізовувати атаку з підстановкою вгаданих значень.

### Генерація гами:
```python
cipher = Strumok()
cipher.initialize(case["key"], case["iv"])

# correctness verification
gamma = cipher.generate_keystream(1)
```

### Можливість задати стан вручну - для реалізації атак.
```python
# the ability to set the internal state directly.
def set_state(self, s_list, r1, r2):
    if len(s_list) != 16:
        raise ValueError("LFSR state must consist of 16 words.")
    self.s = [val & self.MASK_64 for val in s_list]
    self.r1 = r1 & self.MASK_64
    self.r2 = r2 & self.MASK_64
```

### Перевірка на правильність та тест швидкості:
Була написана функція `verify_and_benchmark()` для перевірки правильності та швидкості шифру.
Для перевірки правильності шифру використовувались тестові вектори з додатку Д ДСТУ 8845:2019. Було використано по 4 тестових вектора для Струмок-256 і Струмок-512.

**Приклад використання тестового вектора.**
```python
tests = [
    {
        "name": "Strumok-256 test vector 1",
        "key": [0x0000000000000000,
                0x0000000000000000,
                0x0000000000000000,
                0x8000000000000000],
        "iv": [0x0000000000000000,
               0x0000000000000000,
               0x0000000000000000,
               0x0000000000000000],
        "expected_z0": 0xe442d15345dc66ca
    },
]
```

### Результат тестів:

| Algorithm | Status | Max Speed (Mbps) |
| :--- | :--- | :--- |
| Strumok-256 test vector 1 | PASS | 46.24 |
| Strumok-256 test vector 2 | PASS | 52.90 |
| Strumok-256 test vector 3 | PASS | 52.86 |
| Strumok-256 test vector 4 | PASS | 53.04 |
| Strumok-512 test vector 1 | PASS | 53.09 |
| Strumok-512 test vector 2 | PASS | 53.40 |
| Strumok-512 test vector 3 | PASS | 53.46 |
| Strumok-512 test vector 4 | PASS | 53.74 |

Як можна побачити швидкість шифрування є дещо нижчою ніж зазначена в стандарті. Це тому-що я використовував python для реалізації, який є значно повільніший ніж С.
***
# Базис для атаки часткового вгадування для шифру Strumok та приклад обчислення iнших значень внутрiшнього стану (Анна Ахламова)

AutoGuess показав, що для повного відновлення внутрішнього стану достатньо вгадати лише 7 змінних:
R2_3, S3, S19, S6, S7, R2_8, R2_10

Оскільки кожна змінна має розмір 64 біти, загальна теоретична складність такої атаки становить $2^{7 \times 64} = 2^{448}$, що є меншим за встановлений стандартом поріг стійкості $2^{512}$ для «Струмок-512».

За відомими значеннями Z0–Z10 та цими 7 змінними відновлюються всі 62 змінні стану, але для прикладу в реалізації ми відновили 13 зміних

Наприклад:

з R2_3 обчислюється R1_2; 

з R1_2 і S14 обчислюється R2_1;

з R2_1 обчислюється R1_0;

й так далі

Приклад кроку відновлення S₁₈: Використовується рівняння виходу: $Z_3 = S_{18} \oplus R1_3 \oplus R2_3 \oplus S_3$.
Формула для обчислення:
$$S_{18} = ((Z_3 \oplus S_3 \oplus R2_3) - R1_3) \pmod{2^{64}}$$
***
## Короткий порiвняльний аналiз характеристик шифрiв Струмок iз iншими шифрами (ZUC та ChaCha20). (Нікіта Безверхий)

**Внутрішній стан:** Струмок має потокову архітектуру ЛСЗ + FSM і оперує 64-бітними словами. ЛСЗ складається з 1024 бітів а FSM з 128 бітів. Загальний розмір внутрішнього стану 1152 біти, що є дуже великим показником.
ZUC також має архітектуру ЛСЗ + FSM але має значно менший внутрішній стан - 500 біт.
ChaCha20 має Add - Rotate - XOR архітектуру, що не використовує ЛСЗ його загальний стан 512 біт.

**Стійкість до атак вгадування:**
* Струмок та особливо Струмок-512 має вкрай великий внутрішній стан, що робить його вкрай стійким до атак часткового вгадування (2^512 або точніше 2^488 як вже було продемонстровано).
* ChaCha20 та ZUC мають меншу довжину ключа (256 біт) і менший внутрішній стан тому складність атак часткового вгадування для них 2^256.

**Швидкодія:**
* Струмок буде досить швидким на сучасних більш потужних пристроях архітектури x86-64, завдяки використанню 64-бітних інструкцій та попередньо обчисленим таблицям (S-Box, T-Box).
* ChaCha20 в свою чергу фокусується на швидкості. Він найчастіше використовується на мобільних пристроях та інших не потужних пристроях або в протоколах TLS. На цих простроях він покаже себе в рази краще ніж струмок.
* ZUC проєктувався для використання в вишках мобільного звʼязку. Тому досях широкого використання тільки там і його швидкість оптимальна для цього.

**Загалом** Струмок проєктувався для використання на більш потужних пристроях та має набагато вищу стійкість ніж ChaCha20 та ZUC. Але кожен з цих шифрів має свою сферу застосування і на даний момент є майже незамінним в своїй ніші.



***
# Джерела:
https://www.sciencedirect.com/science/article/pii/S0747717121000584

http://emergentmind.com/papers/1204.4535 (https://arxiv.org/pdf/1204.4535)

https://asecuritysite.com/symmetric/zuc

https://www.vpnunlimited.com/ua/help/cybersecurity/chacha20








